- 搭建 eufy Guardian AI 工程脚手架 guardian-ai/（Anker 黑客松 Y平方队）。
- 依据方案说明书目录树落地：app.py / config(risk_rules.yaml,zones.json) / adapters(eufy_sdk,mock_device) / vision(detector,tracker,features) / agents(risk_agent,health_agent) / core(event,config_loader,context_engine,decision_engine) / data/demo / tests。
- 全链路 mock 模式可跑通（无摄像头/SDK）：MockDevice -> Detector -> Tracker -> ContextEngine -> RiskAgent -> DecisionEngine，输出固定 Event JSON 契约。
- 修复 2 个 bug：config_loader 默认路径算到 core/ 应为 ../config；mock 徘徊者移动幅度过大导致 IoU 跟踪丢 id、停留计时重置（改为门口 ROI 边界小幅摆动，IoU>0.3）。
- pytest 25 passed。四类场景结果：family/delivery=低风险(log)；loiter=高风险(75, ptz+light+record+notify)；occlusion=健康异常(health 40, notify_health)。
- 依赖隔离：venv 在 C:/Users/32277/.workbuddy/binaries/python/envs/guardian，仅装 pyyaml+pytest（核心链路），opencv/ultralytics/streamlit 为可选。
